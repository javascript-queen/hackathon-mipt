import fs from "fs";
import path from "path";
import crypto from "crypto";


export const viteManifestPlugin = (webManifestFile, viteManifestFile, projectDir) => {return {
    // todo compile django-compatible production version of vite manifest file
    // todo integrate it on django side
}};
